import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    box: {
        flexDirection: 'row',
        alignItems: 'center',
        marginTop: 30,
        marginBottom: 10
    },
    disc: {
        fontSize: 25,
        fontWeight: 'bold',
        marginLeft: 10
    }
})